# mkdocs-material + plugins for the argos-edge documentation portal.
# Pinned to patch-version ranges so CI builds stay reproducible and a
# breaking upstream release does not silently land in gh-pages.
mkdocs>=1.5,<2.0
mkdocs-material>=9.5,<10.0
mkdocs-minify-plugin>=0.8,<1.0
pymdown-extensions>=10.7,<11.0
