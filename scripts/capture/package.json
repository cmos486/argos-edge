{
  "name": "argos-edge-capture",
  "version": "1.3.36.6",
  "private": true,
  "description": "Read-only Playwright captures of the argos panel for docs/screenshots/. Captures land in /tmp/argos-captures-pending/ and are NEVER committed automatically -- the operator reviews + sanitizes each PNG before moving approved ones to docs/screenshots/.",
  "scripts": {
    "test": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
